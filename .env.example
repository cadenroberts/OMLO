# ORAM Thesis Project — Environment Variables
# Copy to .env and fill in values as needed.

# Device override (cpu, cuda, mps). Leave blank for auto-detect.
DEVICE=

# Path to CIFAR-10 data directory (default: ./data)
DATA_DIR=./data

# Output directory for experiment results (default: ./results)
RESULTS_DIR=./results
